import { component$ } from '@builder.io/qwik';
import { WorkflowDesigner } from '~/components/administration/workflow-designer';

export default component$(() => {
return (
<div class="container">
<WorkflowDesigner />
</div>
);
});
